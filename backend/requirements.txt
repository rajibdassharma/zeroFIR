fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy[asyncio]>=2.0.0
asyncmy>=0.2.9
pydantic>=2.10.0
email-validator>=2.0.0
pydantic-settings>=2.7.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# bcrypt MUST be pinned <4.1 — 4.1+ removes bcrypt.__about__ which
# passlib 1.7.4 reads on init (produces a noisy "trapped" traceback).
# 5.x breaks CryptContext outright. Lesson from CyberFraud, day one here.
bcrypt>=4.0.1,<4.1
python-multipart>=0.0.18
python-dotenv>=1.0.0
# Production server — Gunicorn with Uvicorn workers
gunicorn>=23.0.0
# PDF generation (transfer receipts, endorsements — Phase 4+)
reportlab>=4.2.0
# HTTP client
httpx>=0.27.0
# Test tooling
pytest>=8.0.0
pytest-asyncio>=0.24.0
# aiosqlite lets the pytest suite run without MySQL. Prod runs asyncmy.
aiosqlite>=0.20.0
